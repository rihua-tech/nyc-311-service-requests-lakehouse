# Local pytest and Ruff checks do not require environment variables.
# Keep Azure, Databricks, and ADF credentials out of this file and source control.
# Cloud runs should use Azure identity, Databricks secret scopes, or deployment-time configuration.

NYC311_ENVIRONMENT=dev
